package com.example.todo_app.client.components.buttons;

import javafx.scene.control.Button;

public class SendButton extends Button {
    public Button sendButton(String text){
        Button button = new Button(text);
        return button;

    }
}
